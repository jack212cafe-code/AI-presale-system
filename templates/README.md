# Templates

No binary `.docx` template is committed yet.

The current scaffold generates proposal documents in code through `lib/proposal.js`. If a customer-facing format becomes stable, add a base template here and update `agents/proposal.js` accordingly.

