---
phase: 04-chat-ui-frontend
plan: 05
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
files_modified: []
autonomous: false
requirements:
  - M2

must_haves:
  truths:
    - "Team member can open browser, log in, type Thai brief, receive solution + BOM table + download link without leaving the chat"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete chat UI flow — login, send message, see solution cards, select option, see BOM table, download proposal.

Purpose: Verify the M2 acceptance criteria end-to-end in a real browser before marking the phase complete.
Output: Confirmation that the chat UI works for the team.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-chat-ui-frontend/04-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete chat UI with login, message thread, solution cards, BOM table, and proposal download</what-built>
  <how-to-verify>
1. Start server: `node server.js`
2. Open browser to `http://localhost:3000/chat`
3. Verify redirect to `/login` (unauthenticated)
4. Log in with a seeded team user account
5. Verify redirect to `/chat` with empty state ("เริ่มต้นการสนทนา")
6. Type a Thai brief (e.g. "ต้องการระบบ HCI สำหรับองค์กร 500 คน พร้อม Backup")
7. Verify loading bubble appears with typing dots and rotating Thai stage labels
8. Verify solution options render as cards with "เลือกตัวเลือกนี้" buttons
9. Click a solution card button
10. Verify cards become disabled after selection
11. Verify BOM renders as a formatted table in the assistant bubble
12. Verify "ดาวน์โหลด Proposal" download button appears
13. Click download — verify DOCX file downloads
14. Verify sidebar shows the new project
15. Click "+ New Chat" — verify thread clears and empty state returns
16. Click the project in sidebar — verify conversation history loads
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Phase exit criteria from ROADMAP: "Team member opens browser, logs in, types Thai brief, receives solution + BOM table + download link without leaving the chat"
</verification>

<success_criteria>
All 16 verification steps pass in a real browser session.
</success_criteria>

<output>
After completion, create `.planning/phases/04-chat-ui-frontend/04-05-SUMMARY.md`
</output>
