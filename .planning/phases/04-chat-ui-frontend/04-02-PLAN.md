---
phase: 04-chat-ui-frontend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - login/login.html
  - login/login.js
autonomous: true
requirements:
  - M2

must_haves:
  truths:
    - "Unauthenticated user sees a login form"
    - "Submitting valid credentials redirects to /chat"
    - "Already-authenticated user visiting /login is redirected to /chat"
    - "Invalid credentials show Thai error message"
  artifacts:
    - path: "login/login.html"
      provides: "Login page with form, warm cream design, centered card"
      min_lines: 40
      contains: "เข้าสู่ระบบ"
    - path: "login/login.js"
      provides: "Session check on load, login form submit handler"
      min_lines: 30
      contains: "api/auth/login"
  key_links:
    - from: "login/login.js"
      to: "/api/auth/session"
      via: "fetch GET on page load"
      pattern: "api/auth/session"
    - from: "login/login.js"
      to: "/api/auth/login"
      via: "fetch POST on form submit"
      pattern: "api/auth/login"
---

<objective>
Create the login page (login/login.html + login/login.js) with session-check-on-load redirect pattern.

Purpose: Provides the authentication entry point for the chat UI. Unauthenticated users are redirected here from /chat.
Output: login/login.html (centered card form) + login/login.js (session check + login POST).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-chat-ui-frontend/04-CONTEXT.md
@.planning/phases/04-chat-ui-frontend/04-RESEARCH.md
@.planning/phases/04-chat-ui-frontend/04-UI-SPEC.md

<interfaces>
<!-- Auth API contract from server.js -->
POST /api/auth/login:
  Request: { username: string, password: string }
  Success 200: { ok: true, user: { id, username, display_name } } + Set-Cookie header
  Failure 401: { ok: false, error: "Invalid credentials" }

GET /api/auth/session:
  Authenticated 200: { ok: true, authenticated: true, user: { id, display_name } }
  Not authenticated 200: { ok: true, authenticated: false }

<!-- CSS :root block from intake/index.html — copy verbatim -->
```css
:root {
  color-scheme: light;
  --bg: #f6f2ea;
  --surface: rgba(255, 255, 255, 0.84);
  --surface-strong: #fffdf9;
  --ink: #171717;
  --muted: #6b7280;
  --line: rgba(23, 23, 23, 0.1);
  --accent: #111111;
  --accent-soft: rgba(17, 17, 17, 0.06);
  --success-bg: #eef6f1;
  --success-ink: #21593e;
  --error-bg: #fbefef;
  --error-ink: #8b2d38;
  --shadow: 0 24px 70px rgba(23, 23, 23, 0.08);
  --radius-xl: 30px;
  --radius-lg: 22px;
  --radius-md: 18px;
}
```

<!-- admin.js patterns to replicate -->
From admin/admin.js:
```javascript
function escapeHtml(value) {
  return String(value || "")
    .replaceAll("&", "&amp;").replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#39;");
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create login/login.html</name>
  <files>login/login.html</files>
  <read_first>
    - admin/index.html (reference for login panel HTML structure and styling)
    - intake/index.html (lines 1-30 for :root CSS variables to copy verbatim)
  </read_first>
  <action>
Create `login/login.html` — a single-page login form with centered card layout. Per D-01b, mirrors admin/index.html login panel pattern. Per D-02, uses warm cream palette from intake/index.html.

**HTML structure:**
```html
<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>AI Presale Assistant — Login</title>
  <style>
    /* Copy :root block verbatim from intake/index.html */
    :root { ... all CSS variables ... }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      color: var(--ink);
      background: var(--bg);
    }
    .login-card {
      background: var(--surface);
      border: 1px solid var(--line);
      border-radius: var(--radius-xl);
      padding: 32px;
      max-width: 400px;
      width: 100%;
      box-shadow: var(--shadow);
    }
    .login-card h1 {
      margin: 0 0 8px;
      font-size: 28px;
      font-weight: 600;
      line-height: 1.2;
    }
    .login-card p {
      margin: 0 0 24px;
      color: var(--muted);
      font-size: 16px;
    }
    label {
      display: block;
      margin-bottom: 4px;
      font-size: 13px;
      color: var(--muted);
      font-weight: 400;
    }
    input {
      width: 100%;
      padding: 13px 14px;
      margin-bottom: 16px;
      border: 1px solid var(--line);
      border-radius: var(--radius-md);
      background: var(--surface-strong);
      font-size: 16px;
      font-family: inherit;
      color: var(--ink);
    }
    input:focus {
      outline: none;
      border-color: rgba(17,17,17,0.26);
      box-shadow: 0 0 0 4px rgba(17,17,17,0.06);
    }
    .primary {
      width: 100%;
      padding: 13px 20px;
      border: none;
      border-radius: var(--radius-md);
      background: var(--accent);
      color: #f7f4ef;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      font-family: inherit;
    }
    .primary:hover { opacity: 0.9; }
    .primary:disabled { opacity: 0.5; cursor: wait; }
    .error-msg {
      display: none;
      margin-bottom: 16px;
      padding: 12px;
      background: var(--error-bg);
      color: var(--error-ink);
      border-radius: var(--radius-md);
      font-size: 14px;
    }
    .error-msg.visible { display: block; }
  </style>
</head>
<body>
  <div class="login-card">
    <h1>AI Presale Assistant</h1>
    <p>เข้าสู่ระบบเพื่อเริ่มต้นใช้งาน</p>
    <div id="error-msg" class="error-msg"></div>
    <form id="login-form" autocomplete="on">
      <label for="username">Username</label>
      <input type="text" id="username" name="username" required autocomplete="username" />
      <label for="password">Password</label>
      <input type="password" id="password" name="password" required autocomplete="current-password" />
      <button type="submit" class="primary" id="login-btn">เข้าสู่ระบบ</button>
    </form>
  </div>
  <script src="/login/login.js"></script>
</body>
</html>
```

Per D-01b: centered card, same inline panel pattern as admin/index.html.
Per D-02: warm cream palette using CSS variables from intake/index.html.
Per UI-SPEC: login card padding 32px, max-width 400px, border-radius var(--radius-xl).
Per UI-SPEC copywriting: CTA button text is "เข้าสู่ระบบ", error text is "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง".
  </action>
  <verify>
    <automated>grep -c "เข้าสู่ระบบ" login/login.html && grep -c "login-form" login/login.html && grep -c "login.js" login/login.html</automated>
  </verify>
  <acceptance_criteria>
    - login/login.html exists with `<!DOCTYPE html>` header
    - Contains `:root` block with `--bg: #f6f2ea` and all CSS variables
    - Contains `<form id="login-form">` with username and password inputs
    - Contains `<button type="submit" class="primary"` with text "เข้าสู่ระบบ"
    - Contains `<div id="error-msg" class="error-msg">`
    - Contains `<script src="/login/login.js">`
    - Body uses `height: 100vh; display: flex; align-items: center; justify-content: center`
    - .login-card has `padding: 32px; max-width: 400px; border-radius: var(--radius-xl)`
  </acceptance_criteria>
  <done>login/login.html renders centered login card with warm cream palette and Thai UI copy</done>
</task>

<task type="auto">
  <name>Task 2: Create login/login.js</name>
  <files>login/login.js</files>
  <read_first>
    - admin/admin.js (lines 1-50 for escapeHtml, apiFetch-like pattern, syncSession pattern)
  </read_first>
  <action>
Create `login/login.js` — session check on page load + login form submit handler. Per D-01c/D-01d: if already authenticated, redirect to /chat. After successful login, redirect to /chat.

```javascript
const loginForm = document.querySelector("#login-form");
const errorMsg = document.querySelector("#error-msg");
const loginBtn = document.querySelector("#login-btn");

async function apiFetch(url, options = {}) {
  const response = await fetch(url, {
    credentials: "include",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...(options.headers || {})
    }
  });
  const payload = await response.json().catch(() => ({}));
  return { response, payload };
}

function showError(message) {
  errorMsg.textContent = message;
  errorMsg.classList.add("visible");
}

function hideError() {
  errorMsg.classList.remove("visible");
}

async function checkSession() {
  try {
    const { response, payload } = await apiFetch("/api/auth/session", { method: "GET" });
    if (response.ok && payload.authenticated) {
      window.location.replace("/chat");
    }
  } catch (e) {
    // Not authenticated — stay on login page
  }
}

loginForm.addEventListener("submit", async (e) => {
  e.preventDefault();
  hideError();
  loginBtn.disabled = true;

  try {
    const { response, payload } = await apiFetch("/api/auth/login", {
      method: "POST",
      body: JSON.stringify({
        username: document.querySelector("#username").value,
        password: document.querySelector("#password").value
      })
    });

    if (!response.ok) {
      showError(payload.error || "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง");
      loginBtn.disabled = false;
      return;
    }

    window.location.replace("/chat");
  } catch (error) {
    showError("ไม่สามารถเชื่อมต่อเซิร์ฟเวอร์ได้");
    loginBtn.disabled = false;
  }
});

checkSession();
```

Per D-01c: /chat checks session → if not auth, redirects to /login (handled in chat.js).
Per D-01d: After login, redirect back to /chat via `window.location.replace("/chat")`.
Per UI-SPEC copywriting: error message "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง".
Per RESEARCH Pattern 7: apiFetch wrapper with `credentials: "include"`.
  </action>
  <verify>
    <automated>grep -c "api/auth/login" login/login.js && grep -c "api/auth/session" login/login.js && grep -c "location.replace" login/login.js</automated>
  </verify>
  <acceptance_criteria>
    - login/login.js contains `async function apiFetch(url, options` with `credentials: "include"`
    - Contains `async function checkSession()` that calls GET /api/auth/session
    - checkSession redirects to `/chat` via `window.location.replace("/chat")` when authenticated
    - Form submit handler calls POST /api/auth/login with `{ username, password }`
    - On success (response.ok): `window.location.replace("/chat")`
    - On failure: calls `showError` with Thai message
    - `checkSession()` called at bottom of file (auto-runs on load)
    - Button disabled during submit, re-enabled on error
  </acceptance_criteria>
  <done>login/login.js checks session on load (redirect to /chat if auth), handles form submit with POST /api/auth/login, shows Thai error on failure</done>
</task>

</tasks>

<verification>
- `test -f login/login.html && test -f login/login.js && echo "Files exist"`
- login.html contains form with id="login-form"
- login.js calls both /api/auth/session and /api/auth/login
</verification>

<success_criteria>
- Login page renders centered card with warm cream palette
- Session check on load redirects authenticated users to /chat
- Form submission sends credentials to POST /api/auth/login
- Successful login redirects to /chat
- Failed login shows Thai error message
</success_criteria>

<output>
After completion, create `.planning/phases/04-chat-ui-frontend/04-02-SUMMARY.md`
</output>
