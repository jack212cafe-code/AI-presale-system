---
phase: 07-discovery-agent-classifier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/discovery.js
  - agents/_prompts/discovery.md
  - lib/validation.js
  - test/unit/discovery.test.js
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03]

must_haves:
  truths:
    - "runDiscoveryAgent with mode generate_questions returns an object with question_text string"
    - "runDiscoveryAgent with mode parse_answers returns full requirements JSON including category and assumptions_applied"
    - "Mock mode returns appropriate objects for both modes"
    - "validateRequirements accepts category and assumptions_applied fields without error"
  artifacts:
    - path: "agents/discovery.js"
      provides: "Two-mode runDiscoveryAgent, discoveryQuestionsFormat schema, extended discoveryTextFormat schema, CATEGORY_TO_USE_CASES mapping"
      exports: ["runDiscoveryAgent", "CATEGORY_TO_USE_CASES"]
    - path: "agents/_prompts/discovery.md"
      provides: "Two-mode prompt: generate_questions and parse_answers"
      contains: "generate_questions"
    - path: "lib/validation.js"
      provides: "Updated validateRequirements accepting category + assumptions_applied"
      contains: "category"
    - path: "test/unit/discovery.test.js"
      provides: "Unit tests for two-mode discovery agent"
      contains: "generate_questions"
  key_links:
    - from: "agents/discovery.js"
      to: "agents/_prompts/discovery.md"
      via: "loadPrompt() reads the prompt file"
      pattern: "loadPrompt"
    - from: "agents/discovery.js"
      to: "lib/validation.js"
      via: "validateRequirements called on parse_answers output"
      pattern: "validateRequirements"
---

<objective>
Extend the discovery agent to support two modes: "generate_questions" (returns a Thai conversational question paragraph) and "parse_answers" (returns full requirements JSON with category + assumptions_applied). Update the prompt, JSON schemas, validation, and mock mode. Add unit tests.

Purpose: Foundation for Phase 7 discovery dialog — the agent must be able to both ask and parse before chat.js can orchestrate the flow.
Output: Two-mode discovery agent with unit test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-discovery-agent-classifier/07-CONTEXT.md
@.planning/phases/07-discovery-agent-classifier/07-RESEARCH.md

@agents/discovery.js
@agents/_prompts/discovery.md
@lib/validation.js
@lib/openai.js

<interfaces>
From agents/discovery.js (current exports):
```javascript
export async function runDiscoveryAgent(intake, options = {}) { ... }
// options: { projectId }
// Returns: validated requirements object
```

From lib/openai.js:
```javascript
export async function generateJsonWithOpenAI({ systemPrompt, userPrompt, model, textFormat, mockResponseFactory }) { ... }
```

From lib/validation.js:
```javascript
export function validateRequirements(output) { ... }
// Asserts: customer_profile, partner_context, use_cases, pain_points, desired_outcomes,
//          trust_requirements, workflow_blockers, recommended_next_questions, success_criteria,
//          scale, constraints, gaps — all required
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unit test scaffold for two-mode discovery agent</name>
  <files>test/unit/discovery.test.js</files>
  <read_first>
    - agents/discovery.js
    - test/unit/getKnowledge.test.js
    - lib/validation.js
  </read_first>
  <behavior>
    - Test 1: runDiscoveryAgent with mode "generate_questions" returns object with non-empty question_text string
    - Test 2: runDiscoveryAgent with mode "parse_answers" returns object with valid category field (one of HCI/DR/Backup/Security/Full-stack)
    - Test 3: runDiscoveryAgent with mode "parse_answers" returns assumptions_applied as an array
    - Test 4: buildMockRequirements with mode "generate_questions" returns { question_text: string }
    - Test 5: buildMockRequirements with mode "parse_answers" returns object with category and assumptions_applied fields
    - Test 6: CATEGORY_TO_USE_CASES maps each category to correct use_cases array
  </behavior>
  <action>
    Create `test/unit/discovery.test.js` using node:test and node:assert/strict (same pattern as test/unit/getKnowledge.test.js).

    Set `process.env.AI_PRESALE_FORCE_LOCAL = "1"` at top so mock mode is used.

    Import `runDiscoveryAgent` and `CATEGORY_TO_USE_CASES` from `../../agents/discovery.js`.

    Test fixture intake object:
    ```javascript
    const testIntake = {
      customer_name: "Test Corp",
      partner_type: "System Integrator",
      primary_use_case: "HCI infrastructure for 100 VMs",
      core_pain_point: "Legacy servers reaching EOL",
      desired_outcome: "Modern hyperconverged platform",
      trust_priority: "Reliability",
      notes: "Need backup solution too",
      users: 200,
      vm_count: 100,
      storage_tb: 50
    };
    ```

    Tests:
    1. `describe("generate_questions mode")` — call `runDiscoveryAgent(testIntake, { mode: "generate_questions" })`, assert result has `question_text` property, assert typeof `question_text` is "string", assert `question_text.length > 0`.
    2. `describe("parse_answers mode")` — call `runDiscoveryAgent(testIntake, { mode: "parse_answers", discoveryReply: "50 VM, 20TB storage, budget 3M THB" })`, assert result has `category` property, assert `["HCI","DR","Backup","Security","Full-stack"].includes(result.category)`, assert `Array.isArray(result.assumptions_applied)`.
    3. `describe("CATEGORY_TO_USE_CASES")` — assert map has keys HCI, DR, Backup, Security, Full-stack. Assert `CATEGORY_TO_USE_CASES["HCI"]` deep equals `["HCI"]`. Assert `CATEGORY_TO_USE_CASES["Full-stack"]` includes "HCI", "Disaster Recovery", "Backup & Recovery", "Cybersecurity".

    These tests WILL FAIL initially (RED) because agents/discovery.js does not yet export CATEGORY_TO_USE_CASES or support mode parameter.
  </action>
  <verify>
    <automated>ls test/unit/discovery.test.js && grep -c "generate_questions" test/unit/discovery.test.js && grep -c "parse_answers" test/unit/discovery.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/unit/discovery.test.js exists
    - File contains `import { runDiscoveryAgent, CATEGORY_TO_USE_CASES }` 
    - File contains `mode: "generate_questions"`
    - File contains `mode: "parse_answers"`
    - File contains at least 5 `it(` or `test(` blocks
    - Tests fail with import/assertion errors (RED phase — agents/discovery.js not yet updated)
  </acceptance_criteria>
  <done>Test file exists with 6 test cases covering both modes, category mapping, and mock behavior. Tests are in RED state awaiting implementation in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Extend discovery agent with two-mode operation, schemas, prompt, and validation</name>
  <files>agents/discovery.js, agents/_prompts/discovery.md, lib/validation.js</files>
  <read_first>
    - agents/discovery.js
    - agents/_prompts/discovery.md
    - lib/validation.js
    - lib/openai.js
    - .planning/phases/07-discovery-agent-classifier/07-CONTEXT.md
    - .planning/phases/07-discovery-agent-classifier/07-RESEARCH.md
  </read_first>
  <action>
    **A. agents/discovery.js changes:**

    1. Add `discoveryQuestionsFormat` schema (per D-08, Pitfall 1 — separate schema for generate_questions mode):
    ```javascript
    const discoveryQuestionsFormat = {
      type: "json_schema",
      name: "discovery_questions_output",
      strict: true,
      schema: {
        type: "object",
        additionalProperties: false,
        properties: {
          question_text: { type: "string" }
        },
        required: ["question_text"]
      }
    };
    ```

    2. Extend `discoveryTextFormat.schema.properties` with two new fields:
    ```javascript
    category: {
      type: "string",
      enum: ["HCI", "DR", "Backup", "Security", "Full-stack"]
    },
    assumptions_applied: {
      type: "array",
      items: { type: "string" }
    }
    ```
    Add both `"category"` and `"assumptions_applied"` to the `required` array.

    3. Add `CATEGORY_TO_USE_CASES` mapping and export it:
    ```javascript
    export const CATEGORY_TO_USE_CASES = {
      "HCI":        ["HCI"],
      "DR":         ["Disaster Recovery"],
      "Backup":     ["Backup & Recovery"],
      "Security":   ["Cybersecurity"],
      "Full-stack": ["HCI", "Disaster Recovery", "Backup & Recovery", "Cybersecurity"]
    };
    ```

    4. Add `DISCOVERY_DEFAULTS` constant (per Claude's discretion from CONTEXT.md):
    ```javascript
    const DISCOVERY_DEFAULTS = {
      vm_count: 50,
      storage_tb: 20,
      users: 100,
      budget_range_thb: "3,000,000-5,000,000 THB",
      network: "10G"
    };
    ```

    5. Update `buildMockRequirements(intake, mode)` to accept `mode` parameter:
    - When `mode === "generate_questions"`: return `{ question_text: "สวัสดีครับ ผมเข้าใจว่าคุณต้องการระบบ... ช่วยบอกรายละเอียดเพิ่มเติมได้ไหมครับ เช่น มี VM อยู่ประมาณกี่ตัว storage รวมประมาณกี่ TB จำนวน user ทั้งหมด network switch ที่ใช้อยู่เป็น 10G หรือ 25G และงบประมาณคร่าวๆ ครับ" }`
    - When `mode !== "generate_questions"` (default/parse_answers): return existing mock requirements object with additional fields `category: "HCI"` and `assumptions_applied: []`

    6. Update `sanitizeRequirements(output, intake)` to pass through new fields:
    - Add `category: requirements.category ?? null` to the return object
    - Add `assumptions_applied: toArray(requirements.assumptions_applied)` to the return object

    7. Refactor `runDiscoveryAgent(intake, options = {})`:
    - Extract `mode` from `options.mode` (default: `"parse_answers"`)
    - Extract `discoveryReply` from `options.discoveryReply` (default: `null`)
    - Choose `textFormat`: if `mode === "generate_questions"` use `discoveryQuestionsFormat`, else use `discoveryTextFormat`
    - Build `userPrompt`: if `mode === "generate_questions"`, send `JSON.stringify({ mode: "generate_questions", brief: intake }, null, 2)`. If `mode === "parse_answers"`, send `JSON.stringify({ mode: "parse_answers", brief: intake, discovery_reply: discoveryReply, defaults: DISCOVERY_DEFAULTS }, null, 2)`.
    - Update `mockResponseFactory`: `async () => mode === "generate_questions" ? buildMockRequirements(intake, "generate_questions") : buildMockRequirements(intake, "parse_answers")`
    - IMPORTANT: Both modes call `generateJsonWithOpenAI()` which already logs to agent_logs via the standard wrapper. Confirm that the `generateJsonWithOpenAI` call path is used for BOTH modes (not bypassed for generate_questions). This ensures CLAUDE.md compliance: "Log every LLM call to agent_logs".
    - For `generate_questions` mode: return the raw output directly (just `{ question_text }` — no sanitize/validate)
    - For `parse_answers` mode: apply existing `sanitizeRequirements` + `validateRequirements` pipeline
    - After parse_answers validation, map category to use_cases: `result.use_cases = CATEGORY_TO_USE_CASES[result.category] ?? deriveUseCases(intake)` — this ensures the downstream solution agent gets correctly routed use_cases (per D-14).

    **B. agents/_prompts/discovery.md changes:**

    Replace the entire prompt content with a two-mode prompt. Keep the empathetic DNA framing. The new prompt structure:

    ```markdown
    You are the Discovery Agent for a Thai IT presale company. Your company sells infrastructure solutions covering HCI, DR, Backup, Security, and full-stack combinations. Empathy is the core DNA — assume the user may be overloaded, generalist, or unable to describe the requirement cleanly.

    You operate in two modes based on the `mode` field in the user input JSON.

    ## Mode: generate_questions

    When `mode` is `"generate_questions"`:
    1. Read the `brief` field from user input.
    2. Identify which data points are ALREADY known from the brief.
    3. Return ONLY: `{ "question_text": "..." }`
    4. Write question_text as a single warm conversational Thai paragraph. You are a Thai presale engineer speaking to a colleague. Be empathetic, not clinical.
    5. Cover ONLY the MISSING data points from this list:
       - Use case type (HCI / DR / Backup / Security / combination)
       - VM count and storage estimate (TB)
       - User count (for licensing)
       - Existing infrastructure (network switch 10G/25G, existing NAS/storage)
       - Budget range (THB)
    6. Do NOT number the questions. Do NOT use a form or list format. Weave questions naturally into flowing Thai text.
    7. If the brief already provides a data point, do NOT re-ask it.

    ## Mode: parse_answers

    When `mode` is `"parse_answers"`:
    1. Read both `brief` and `discovery_reply` from user input.
    2. Extract all known values from both texts combined. Thai free-text input — no regex, parse with understanding.
    3. For any field still unknown after reading both texts, apply the industry-standard default from the `defaults` object and record each applied default in `assumptions_applied[]` as a Thai string (e.g., "ใช้ค่าเริ่มต้น: VM 50 ตัว").
    4. Classify the request as exactly one of: HCI | DR | Backup | Security | Full-stack — store in `category`. If multiple categories are clearly indicated, use `Full-stack`.
    5. Return the full requirements JSON schema.

    Return valid JSON only.

    Full requirements schema fields:
    - customer_profile (name, industry, environment)
    - partner_context (partner_type, operating_model, engagement_motion)
    - use_cases (array of strings)
    - pain_points (array)
    - desired_outcomes (array)
    - trust_requirements (array)
    - workflow_blockers (array)
    - recommended_next_questions (array)
    - success_criteria (array)
    - scale (users, vm_count, storage_tb)
    - budget_range (string or null)
    - timeline (string or null)
    - constraints (array)
    - gaps (array)
    - source_mode (string — set to "live")
    - category (one of: HCI, DR, Backup, Security, Full-stack)
    - assumptions_applied (array of strings — each default applied)

    Discovery priorities:
    - Extract the user's likely intent even when the brief is short, messy, or partially contradictory.
    - Help structure unclear user input into something usable instead of expecting perfect requirements.
    - Make uncertainty visible in a supportive way and capture missing info in `gaps`.
    - Keep `recommended_next_questions` short and practical.
    - Keep the output grounded in what is known from the intake; do not invent detailed technical facts.
    - If data is missing, use `null` and record the missing point in `gaps`.
    ```

    **C. lib/validation.js changes:**

    In `validateRequirements(output)`:
    - Add after the `gaps` assertion: `if (output.category !== undefined) { assert(typeof output.category === "string", "requirements.category must be a string"); }`
    - Add: `if (output.assumptions_applied !== undefined) { assert(Array.isArray(output.assumptions_applied), "requirements.assumptions_applied must be an array"); }`
    - These are optional assertions — the fields are accepted but not required, so existing code calling validateRequirements without these fields still passes.
  </action>
  <verify>
    <automated>node --test --test-isolation=none test/unit/discovery.test.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - agents/discovery.js contains `export const CATEGORY_TO_USE_CASES`
    - agents/discovery.js contains `discoveryQuestionsFormat`
    - agents/discovery.js contains `mode === "generate_questions"`
    - agents/discovery.js contains `assumptions_applied`
    - agents/discovery.js contains `category` in discoveryTextFormat schema properties
    - agents/_prompts/discovery.md contains `Mode: generate_questions` and `Mode: parse_answers`
    - agents/_prompts/discovery.md contains Thai language instructions for question generation
    - lib/validation.js contains `output.category` assertion
    - lib/validation.js contains `output.assumptions_applied` assertion
    - Both modes use `generateJsonWithOpenAI()` call path (agent_logs logging per CLAUDE.md)
    - `node --test --test-isolation=none test/unit/discovery.test.js` exits 0 (all 6 tests GREEN)
    - `node --test --test-isolation=none` (full suite) exits 0 — no regressions
  </acceptance_criteria>
  <done>Discovery agent supports two modes. Mock mode works for both. Category + assumptions_applied fields flow through schema, sanitize, and validate. CATEGORY_TO_USE_CASES exported. Both modes log to agent_logs via generateJsonWithOpenAI wrapper. All unit tests pass. Full test suite has no regressions.</done>
</task>

</tasks>

<verification>
- `node --test --test-isolation=none test/unit/discovery.test.js` — all 6+ tests pass
- `node --test --test-isolation=none` — full suite green (no regressions)
- `grep -c "generate_questions" agents/discovery.js` returns >= 2
- `grep -c "CATEGORY_TO_USE_CASES" agents/discovery.js` returns >= 2
- `grep -c "assumptions_applied" agents/discovery.js` returns >= 2
- `grep -c "generateJsonWithOpenAI" agents/discovery.js` returns >= 1 (confirms agent_logs path for both modes)
</verification>

<success_criteria>
- runDiscoveryAgent(intake, { mode: "generate_questions" }) returns { question_text: "..." } in mock mode
- runDiscoveryAgent(intake, { mode: "parse_answers", discoveryReply: "..." }) returns requirements with category + assumptions_applied in mock mode
- CATEGORY_TO_USE_CASES maps all 5 categories correctly
- validateRequirements accepts both old-format (no category) and new-format (with category) requirements
- Both discovery modes route through generateJsonWithOpenAI, ensuring agent_logs compliance
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-discovery-agent-classifier/07-01-SUMMARY.md`
</output>
