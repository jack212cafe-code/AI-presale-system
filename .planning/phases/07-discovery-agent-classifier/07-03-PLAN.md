---
phase: 07-discovery-agent-classifier
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: []
autonomous: false
requirements: [DISC-01, DISC-02, DISC-03, DISC-04]

must_haves:
  truths:
    - "Discovery questions appear as flowing Thai paragraph, not a numbered list"
    - "All five data points are covered in the discovery question message"
    - "Category is correctly classified for each test scenario"
    - "Default assumptions are visible in chat when user omits answers"
    - "Full round-trip completes in one user response without blocking interview"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete discovery dialog flow. Confirm that the LLM-generated Thai questions are conversational, the classifier works correctly, and the default assumptions are visible.

Purpose: Validate tone/UX quality that cannot be asserted by automated tests (D-05, D-06 empathetic Thai tone; D-07 all data points covered; DISC-03 visible defaults).
Output: Human approval or issue list for iteration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-discovery-agent-classifier/07-CONTEXT.md
@.planning/phases/07-discovery-agent-classifier/07-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify discovery dialog quality and flow</name>
  <files>lib/chat.js, agents/_prompts/discovery.md</files>
  <read_first>
    - lib/chat.js
    - agents/_prompts/discovery.md
  </read_first>
  <action>
    This is a human verification checkpoint. What was built:

    Complete discovery dialog flow: first message triggers LLM-generated Thai discovery questions, user replies in one message, system classifies request, applies defaults for missing data, announces assumptions, then proceeds to solution generation.

    Start the server with real OpenAI credentials (not mock mode). Test these scenarios:

    **Scenario 1: Full information provided**
    1. POST /api/chat with message: "ต้องการระบบ HCI สำหรับ 100 VM storage 50TB user 200 คน network 25G งบ 8 ล้าน"
    2. Verify: Discovery questions should be minimal (most data already provided)
    3. Reply with remaining details
    4. Verify: Stage proceeds to awaiting_selection with solution options
    5. Verify: category should be "HCI"

    **Scenario 2: Minimal brief with defaults (DISC-03)**
    1. POST /api/chat with message: "อยากได้ระบบ backup"
    2. Verify: Discovery questions cover all 5 data points as flowing Thai paragraph (not numbered list)
    3. Reply: "ไม่รู้เลยครับ ขอแบบมาตรฐาน"
    4. Verify: Assumption announcement message appears naming each default used
    5. Verify: Stage proceeds to awaiting_selection
    6. Verify: category should be "Backup"

    **Scenario 3: DR classification**
    1. POST /api/chat with message: "ต้องการระบบ DR สำหรับ data center"
    2. Reply with discovery answers
    3. Verify: category is "DR" (not Full-stack)

    **Check tone (D-05, D-06):**
    - Questions read as natural Thai conversation, not a clinical questionnaire
    - Tone is warm and empathetic, like a Thai presale engineer colleague
    - No numbered lists, no form-like structure

    **Check data points (D-07):**
    - Use case type, VM count + storage, user count, existing infrastructure (network), budget range

    Example curl:
    ```bash
    curl -X POST http://localhost:3000/api/chat \
      -H "Content-Type: application/json" \
      -H "Cookie: user_session=YOUR_SESSION_TOKEN" \
      -d '{"message": "อยากได้ระบบ backup"}'
    ```

    Resume signal: Type "approved" if all scenarios pass, or describe issues to fix.
  </action>
  <verify>
    <automated>node --test --test-isolation=none 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Discovery questions are a single Thai paragraph (not numbered list)
    - All 5 data points are asked about when not provided in brief
    - "ไม่รู้" answer triggers visible default assumption message
    - Category classification matches the brief intent for all 3 scenarios
    - Full flow completes: brief -> questions -> reply -> solution options
  </acceptance_criteria>
  <done>Human confirms discovery dialog quality: Thai tone is conversational, all data points covered, defaults announced, classifier accurate, single-round flow works.</done>
</task>

</tasks>

<verification>
Human verification — automated tests already green from Plans 01 and 02. Full suite run as sanity check.
</verification>

<success_criteria>
- Human approves discovery question tone and format
- Human confirms classifier accuracy for HCI, Backup, and DR scenarios
- Human confirms default assumption visibility
- Human confirms single-round completion (no multi-step blocking)
</success_criteria>

<output>
After completion, create `.planning/phases/07-discovery-agent-classifier/07-03-SUMMARY.md`
</output>
