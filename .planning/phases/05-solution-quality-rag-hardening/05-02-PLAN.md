---
phase: 05-solution-quality-rag-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/_prompts/solution.md
  - agents/_prompts/proposal.md
autonomous: true
requirements: [M5]

must_haves:
  truths:
    - "Solution prompt frames the agent as IT presale for Thai enterprise (not SaaS)"
    - "Solution prompt references all 5 domains: HCI, 3-Tier, Backup & Recovery, DR, Cybersecurity"
    - "Solution prompt names all 6 vendor families: Nutanix, Veeam, Fortinet, Dell, Cisco, HPE"
    - "Solution prompt instructs component-level architecture with rationale"
    - "Proposal prompt instructs all output in Thai"
    - "Proposal prompt provides field-level guidance for all 4 fields"
  artifacts:
    - path: "agents/_prompts/solution.md"
      provides: "IT presale solution prompt for Thai enterprise"
      contains: "Nutanix"
    - path: "agents/_prompts/proposal.md"
      provides: "Thai enterprise proposal prompt with field guidance"
      contains: "ภาษาไทย"
  key_links:
    - from: "agents/solution.js"
      to: "agents/_prompts/solution.md"
      via: "loadPrompt reads solution.md as system prompt"
      pattern: "loadPrompt"
    - from: "agents/proposal.js"
      to: "agents/_prompts/proposal.md"
      via: "loadPrompt reads proposal.md as system prompt"
      pattern: "loadPrompt"
---

<objective>
Rewrite the solution prompt and proposal prompt to produce Thai enterprise IT presale quality output instead of generic SaaS output.

Purpose: Addresses M5 — solution reads like a real presale recommendation, proposal is Thai enterprise grade.
Output: Two rewritten prompt files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-solution-quality-rag-hardening/05-CONTEXT.md
@.planning/phases/05-solution-quality-rag-hardening/05-RESEARCH.md

<interfaces>
<!-- Schema constraints the prompts must respect — DO NOT instruct LLM to add fields outside these schemas -->

From agents/solution.js (solutionTextFormat, strict: true, additionalProperties: false):
```javascript
// Each option MUST have exactly these fields:
{
  name: string,
  architecture: string,        // component-level description with rationale
  vendor_stack: string[],      // vendor names from pricing_catalog
  rationale: string[],         // why this fits
  risks: string[],             // known risks
  estimated_tco_thb: number|null  // THB estimate
}
// Top level: { options: [...], selected_option: integer|null, notes: string[] }
```

From agents/proposal.js (proposalTextFormat, strict: true, additionalProperties: false):
```javascript
// Proposal MUST have exactly these 4 string fields:
{
  executive_summary: string,
  solution_overview: string,
  assumptions: string,
  next_steps: string
}
```

loadPrompt resolution paths (confirmed from source):
```javascript
// agents/solution.js line 18:
return readFile(path.join(__dirname, "_prompts", "solution.md"), "utf8");

// agents/proposal.js line 17:
return readFile(path.join(__dirname, "_prompts", "proposal.md"), "utf8");
```
These paths resolve to `agents/_prompts/solution.md` and `agents/_prompts/proposal.md` respectively. The rewritten files MUST be at these exact paths or loadPrompt will silently fail.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite solution prompt for Thai enterprise IT presale</name>
  <files>agents/_prompts/solution.md</files>
  <read_first>agents/_prompts/solution.md, agents/solution.js, scripts/seed-pricing.js</read_first>
  <action>
**Before writing:** Confirm that `agents/solution.js` loadPrompt reads from `path.join(__dirname, "_prompts", "solution.md")` (line 18). The rewritten file MUST be placed at `agents/_prompts/solution.md` to match this path.

**Full replacement** of `agents/_prompts/solution.md`. The new prompt must contain ALL of the following sections. Per D-04, D-05, D-06:

**Role (line 1):**
```
You are a Solution Design Agent for IT infrastructure presale serving Thai enterprise customers.
```

**Domain coverage:**
```
You cover five IT infrastructure domains:
1. Hyper-Converged Infrastructure (HCI)
2. 3-Tier Architecture (Server + Storage + Network)
3. Backup & Recovery
4. Disaster Recovery (DR)
5. Cybersecurity (NGFW, SOC, centralized management)
```

**Vendor catalog:**
```
Available vendors and product families (all have SKUs in pricing_catalog):
- Nutanix: HCI platform, software-only, cloud connectivity, DR replication
- Veeam: Backup standard/enterprise/enterprise-plus, M365 backup, immutable backup
- Dell: Rack servers, 3-tier platforms, SAN storage, VxRail HCI
- Cisco: Switches, routers, HyperFlex HCI, Firepower NGFW
- Fortinet: FortiGate NGFW (200F/600F), FortiAnalyzer, FortiManager
- HPE: ProLiant servers, SimpliVity HCI, MSA storage

Always recommend from these vendors. Do not hallucinate products outside this catalog.
```

**Thai enterprise buying context:**
```
Thai enterprise buying context:
- Budgets are in THB (Thai Baht). estimated_tco_thb must be a realistic number.
- Local support availability matters — prefer vendors with Thai distributor presence.
- Budget sensitivity is high — include a cost-optimized option when budget_range is tight.
- Customers value local references and proven deployments in Thailand.
```

**Architecture rule (per D-06):**
```
Architecture description rule:
Each option's "architecture" field MUST name specific components and tiers with a 1-2 sentence rationale.
Example: "3-node Nutanix NX cluster with Veeam Backup & Replication for immutable backup. This provides a single-vendor HCI platform with enterprise-grade data protection suitable for the customer's 140 VM workload."
Do NOT write vague descriptions like "HCI platform with backup solution".
```

**Output format:**
```
Return valid JSON only matching the required schema.

Output fields:
- options: 2 to 3 solution options (array of objects)
- selected_option: index of recommended option (integer, 0-based)
- notes: array of strings — assumptions, caveats, or context for the recommendation

Each option must include:
- name: descriptive option name
- architecture: component-level description with rationale (see rule above)
- vendor_stack: array of vendor names used in this option
- rationale: array of reasons why this option fits the requirements
- risks: array of known risks or considerations
- estimated_tco_thb: estimated total cost of ownership in THB (number or null if insufficient data)

Do NOT add fields beyond this schema. The schema is enforced with strict validation.
```

**KB and incomplete requirements handling:**
```
Use the provided knowledge base chunks as evidence for your recommendations. Reference specific product capabilities mentioned in KB when justifying vendor choices.

When requirements are incomplete, state assumptions explicitly in the "notes" array. Do not refuse to provide options — make practical assumptions and call them out.
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p=fs.readFileSync('agents/_prompts/solution.md','utf8'); const checks=['Nutanix','Veeam','Fortinet','Dell','Cisco','HPE','HCI','3-Tier','Backup','Disaster Recovery','Cybersecurity','Thai','estimated_tco_thb','architecture']; const missing=checks.filter(c=>!p.includes(c)); if(missing.length){console.log('MISSING:',missing);process.exit(1)}else{console.log('All checks passed');process.exit(0)}"</automated>
  </verify>
  <acceptance_criteria>
    - agents/_prompts/solution.md does NOT contain "SaaS" or "cloud SaaS" or "tenant isolation"
    - agents/_prompts/solution.md contains "Thai enterprise" (per D-04)
    - agents/_prompts/solution.md contains all 6 vendor names: "Nutanix", "Veeam", "Fortinet", "Dell", "Cisco", "HPE" (per D-05)
    - agents/_prompts/solution.md contains all 5 domains: "HCI", "3-Tier", "Backup", "Disaster Recovery", "Cybersecurity" (per D-05)
    - agents/_prompts/solution.md contains "component" or "specific components" (per D-06 architecture rule)
    - agents/_prompts/solution.md contains "Do NOT add fields" or equivalent schema warning (per Pitfall 3)
    - agents/_prompts/solution.md contains "estimated_tco_thb" (THB pricing context)
    - agents/_prompts/solution.md contains "knowledge base" reference (KB grounding instruction)
  </acceptance_criteria>
  <done>Solution prompt fully rewritten for Thai enterprise IT presale with all 5 domains, 6 vendors, component-level architecture rule, and schema compliance warning</done>
</task>

<task type="auto">
  <name>Task 2: Expand proposal prompt for Thai enterprise with field-level guidance</name>
  <files>agents/_prompts/proposal.md</files>
  <read_first>agents/_prompts/proposal.md, agents/proposal.js</read_first>
  <action>
**Before writing:** Confirm that `agents/proposal.js` loadPrompt reads from `path.join(__dirname, "_prompts", "proposal.md")` (line 17). The rewritten file MUST be placed at `agents/_prompts/proposal.md` to match this path.

**Full replacement** of `agents/_prompts/proposal.md`. Expand from 7-line stub to full guidance. Per D-07, D-08, D-09:

**New content:**
```markdown
You are a Proposal Agent for IT infrastructure presale serving Thai enterprise customers.

CRITICAL: All field values MUST be written in Thai (ภาษาไทย) regardless of the input language. Use formal Thai enterprise register (ภาษาทางการ).

Return valid JSON only matching the required schema. Do NOT add fields beyond the 4 required fields.

Draft enterprise proposal content with these 4 fields:

## executive_summary
- 2-3 paragraphs in formal Thai
- Paragraph 1: State the customer's business problem and IT challenge
- Paragraph 2: Summarize the recommended solution approach and key components
- Paragraph 3: Expected business outcome and value delivered
- Keep concise — this is for executive decision-makers who skim

## solution_overview
- Reference the selected option's architecture by name and specific components
- Describe how each component addresses a specific requirement
- Include vendor names and product families
- Write in formal Thai technical register

## assumptions
- List all technical and commercial assumptions made in the proposal
- Include infrastructure assumptions (power, cooling, rack space, network)
- Include commercial assumptions (pricing validity period, support terms, licensing model)
- Each assumption as a separate bullet point within the string

## next_steps
- Provide actionable items with clear ownership for each step
- Use role prefixes: "ฝ่ายขาย:" (Sales), "วิศวกร:" (Engineer), "ลูกค้า:" (Customer)
- Include timeline suggestions (e.g., "ภายใน 2 สัปดาห์")
- Typical steps: detailed design workshop, POC planning, formal quotation, procurement process

The final .docx file will be generated by code.
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p=fs.readFileSync('agents/_prompts/proposal.md','utf8'); const checks=['ภาษาไทย','executive_summary','solution_overview','assumptions','next_steps','ฝ่ายขาย','วิศวกร','ลูกค้า']; const missing=checks.filter(c=>!p.includes(c)); if(missing.length){console.log('MISSING:',missing);process.exit(1)}else{console.log('All checks passed');process.exit(0)}"</automated>
  </verify>
  <acceptance_criteria>
    - agents/_prompts/proposal.md contains "ภาษาไทย" (Thai language instruction per D-07)
    - agents/_prompts/proposal.md contains "ภาษาทางการ" (formal register per D-09)
    - agents/_prompts/proposal.md contains "MUST be written in Thai" or equivalent at the TOP of the file, not buried (per Pitfall 4)
    - agents/_prompts/proposal.md contains field names: "executive_summary", "solution_overview", "assumptions", "next_steps" (per D-08)
    - agents/_prompts/proposal.md contains "2-3 paragraphs" for executive_summary (per D-09)
    - agents/_prompts/proposal.md contains "ฝ่ายขาย" and "วิศวกร" and "ลูกค้า" (action owner prefixes per D-09)
    - agents/_prompts/proposal.md contains "Do NOT add fields" or equivalent schema warning
    - agents/_prompts/proposal.md is longer than 20 lines (expanded from 7-line stub)
  </acceptance_criteria>
  <done>Proposal prompt expanded with Thai language instruction, formal register, field-level content guidance for all 4 fields, and action owner prefixes in next_steps</done>
</task>

</tasks>

<verification>
- Solution prompt has zero SaaS references, full vendor/domain coverage
- Proposal prompt instructs Thai output at the top, has field-level guidance
- `node scripts/smoke.js` still passes (prompts are read at runtime, no syntax to break)
</verification>

<success_criteria>
- Solution prompt rewritten per D-04/D-05/D-06
- Proposal prompt expanded per D-07/D-08/D-09
- No schema violations introduced (no new fields instructed)
</success_criteria>

<output>
After completion, create `.planning/phases/05-solution-quality-rag-hardening/05-02-SUMMARY.md`
</output>
