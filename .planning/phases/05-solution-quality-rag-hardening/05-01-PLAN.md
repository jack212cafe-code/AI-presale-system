---
phase: 05-solution-quality-rag-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fixtures/scenario_cybersecurity.json
  - test/fixtures/scenario_fullstack.json
  - agents/solution.js
autonomous: true
requirements: [S1]

must_haves:
  truths:
    - "getKnowledge runs one embedding call per use_case (not one combined call)"
    - "Results are deduplicated by chunk ID, keeping highest similarity score per ID"
    - "Final chunk count passed to solution agent is capped at 5"
    - "Local fallback mirrors per-use-case loop with source_key dedup"
    - "retrieval_mode field is present in solution output"
  artifacts:
    - path: "agents/solution.js"
      provides: "Per-use-case retrieval in getKnowledge"
      contains: "Promise.allSettled"
    - path: "test/fixtures/scenario_cybersecurity.json"
      provides: "Cybersecurity BOM scenario fixture"
      contains: "Cybersecurity"
    - path: "test/fixtures/scenario_fullstack.json"
      provides: "Full-stack BOM scenario fixture"
      contains: "use_cases"
  key_links:
    - from: "agents/solution.js"
      to: "lib/openai.js"
      via: "embedQuery called once per use_case"
      pattern: "embedQuery\\("
    - from: "agents/solution.js"
      to: "lib/supabase.js"
      via: "retrieveKnowledgeFromVector called once per use_case"
      pattern: "retrieveKnowledgeFromVector\\("
---

<objective>
Refactor getKnowledge() in agents/solution.js from single combined query to per-use-case separate embedding queries with deduplication. Create 2 missing test fixtures for BOM validation.

Purpose: Addresses S1 (per-use-case retrieval) and prepares fixtures for M5 BOM validation.
Output: Refactored getKnowledge, 2 new fixture files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-solution-quality-rag-hardening/05-CONTEXT.md
@.planning/phases/05-solution-quality-rag-hardening/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From agents/solution.js:
```javascript
// getKnowledge signature — MUST keep same signature and return shape
async function getKnowledge(requirements) // requirements.use_cases is string[]
// Returns: { chunks: Array<{id, title, content, similarity}>, retrieval_mode: "vector"|"local_fallback" }

// Public API — signature unchanged
export async function runSolutionAgent(requirements, options = {})
```

From lib/openai.js:
```javascript
export async function embedQuery(text) // returns Float64Array embedding
```

From lib/supabase.js:
```javascript
export async function retrieveKnowledgeFromVector(embedding, matchCount) // returns Array<{id, title, content, similarity}>
```

From knowledge_base/shared.js:
```javascript
export function retrieveLocalKnowledge(query, n) // returns Array<{source_key, title, content, score}>
// NOTE: local entries have source_key, NOT id
```

From lib/config.js:
```javascript
export function hasEmbeddingConfig() // returns boolean
export function hasSupabaseAdmin() // returns boolean
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create missing test fixtures</name>
  <files>test/fixtures/scenario_cybersecurity.json, test/fixtures/scenario_fullstack.json</files>
  <read_first>test/fixtures/scenario_hci.json, test/fixtures/scenario_backup.json, test/fixtures/scenario_dr.json</read_first>
  <action>
Create two new JSON fixture files matching the exact shape of existing fixtures (same keys as scenario_hci.json).

**scenario_cybersecurity.json:**
```json
{
  "customer_name": "SecureBank Corp",
  "partner_type": "System Integrator",
  "industry": "Finance",
  "primary_use_case": "Network security modernization with NGFW and SOC",
  "core_pain_point": "Legacy firewall cannot handle modern threat landscape, no centralized logging.",
  "desired_outcome": "Next-gen firewall HA pair with centralized security management and logging.",
  "output_preference": "Solution options",
  "trust_priority": "Security compliance first",
  "integration_needs": "Exportable proposal for security review board",
  "users": 500,
  "vm_count": 0,
  "storage_tb": 5,
  "budget_range_thb": "2M-4M THB",
  "timeline": "Q3 2026",
  "notes": "Needs NGFW HA pair, centralized logging, policy management. FortiGate preferred."
}
```

**scenario_fullstack.json:**
```json
{
  "customer_name": "TotalTech Enterprise",
  "partner_type": "Direct Customer",
  "industry": "Government",
  "primary_use_case": "Complete IT infrastructure modernization covering compute, backup, DR, and security",
  "core_pain_point": "Aging infrastructure across all layers, no integrated backup or DR strategy.",
  "desired_outcome": "End-to-end modern infrastructure with HCI, backup, DR, and network security.",
  "output_preference": "Solution options",
  "trust_priority": "Accuracy first",
  "integration_needs": "Full proposal document for procurement committee",
  "users": 1000,
  "vm_count": 200,
  "storage_tb": 120,
  "budget_range_thb": "8M-15M THB",
  "timeline": "Q4 2026",
  "notes": "Requires HCI platform, immutable backup, cross-site DR, and NGFW. Multi-vendor acceptable."
}
```
  </action>
  <verify>
    <automated>node -e "const c=require('./test/fixtures/scenario_cybersecurity.json'); const f=require('./test/fixtures/scenario_fullstack.json'); console.log('cyber:', Object.keys(c).length, 'full:', Object.keys(f).length); process.exit(Object.keys(c).length >= 10 && Object.keys(f).length >= 10 ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - test/fixtures/scenario_cybersecurity.json exists and contains "customer_name" key with value "SecureBank Corp"
    - test/fixtures/scenario_fullstack.json exists and contains "customer_name" key with value "TotalTech Enterprise"
    - Both files have all keys present in scenario_hci.json (customer_name, partner_type, industry, primary_use_case, core_pain_point, desired_outcome, output_preference, trust_priority, integration_needs, users, vm_count, storage_tb, budget_range_thb, timeline, notes)
    - scenario_cybersecurity.json notes mention "NGFW"
    - scenario_fullstack.json notes mention "HCI" and "DR" and "NGFW"
  </acceptance_criteria>
  <done>Two fixture files exist with correct shape matching existing fixtures</done>
</task>

<task type="auto">
  <name>Task 2: Refactor getKnowledge to per-use-case retrieval with dedup</name>
  <files>agents/solution.js</files>
  <read_first>agents/solution.js, lib/openai.js, lib/supabase.js, knowledge_base/shared.js, lib/config.js</read_first>
  <action>
Refactor the `getKnowledge(requirements)` function in `agents/solution.js` (lines 64-79). Keep the same function signature and return shape `{ chunks, retrieval_mode }`.

**Replace the function body with:**

1. Extract `useCases` from `requirements.use_cases` (already a string array).

2. **Vector path** (when `hasSupabaseAdmin() && hasEmbeddingConfig()`):
   - Use `Promise.allSettled()` (NOT `Promise.all` — per Pitfall 6) to run parallel calls:
     ```javascript
     const results = await Promise.allSettled(
       useCases.map(async (useCase) => {
         const embedding = await embedQuery(useCase);
         return retrieveKnowledgeFromVector(embedding, 5);
       })
     );
     ```
   - Filter for `status === "fulfilled"`, extract `.value` arrays.
   - If zero fulfilled results, log warning and fall through to local fallback.
   - Deduplicate using `Map<chunk.id, chunk>`: for each chunk, keep the one with the highest `similarity` score.
   - Sort descending by `similarity`, slice to top 5.
   - Return `{ chunks, retrieval_mode: "vector" }`.

3. **Local fallback path** (same as before but per-use-case):
   - Use `Promise.allSettled()` over `useCases.map(uc => retrieveLocalKnowledge(uc, 5))`.
   - Filter for fulfilled results.
   - Deduplicate using `Map<chunk.source_key, chunk>` (local entries have `source_key`, NOT `id`). Keep the entry with the highest `score` (or first encountered if no score).
   - Sort descending by `score` (default 0 if missing), slice to top 5.
   - Return `{ chunks, retrieval_mode: "local_fallback" }`.

4. Wrap the entire vector try block around the `Promise.allSettled` call. If the try block fails or yields zero fulfilled results, fall through to local fallback with a `console.warn`.

**Do NOT change:**
- `runSolutionAgent` signature or behavior
- `sanitizeSolution`, `validateSolution`, `buildMockSolution`
- `solutionTextFormat` schema
- Any imports except if needed

Per D-01, D-02, D-03.
  </action>
  <verify>
    <automated>node -e "import('./agents/solution.js').then(m => { console.log('runSolutionAgent:', typeof m.runSolutionAgent); process.exit(typeof m.runSolutionAgent === 'function' ? 0 : 1) })"</automated>
  </verify>
  <acceptance_criteria>
    - agents/solution.js contains `Promise.allSettled` (not Promise.all for the per-use-case calls)
    - agents/solution.js contains `useCases.map` or `requirements.use_cases.map` calling embedQuery per element
    - agents/solution.js contains `byId.get(chunk.id)` or equivalent Map-based dedup by chunk.id for vector path
    - agents/solution.js contains dedup by `source_key` for local fallback path (grep: `source_key`)
    - agents/solution.js contains `.slice(0, 5)` to cap chunks at 5
    - agents/solution.js still exports `runSolutionAgent`
    - agents/solution.js still imports `embedQuery` from `../lib/openai.js`
    - agents/solution.js still imports `retrieveKnowledgeFromVector` from `../lib/supabase.js`
    - agents/solution.js still imports `retrieveLocalKnowledge` from `../knowledge_base/shared.js`
    - `node scripts/smoke.js` passes (existing test suite)
  </acceptance_criteria>
  <done>getKnowledge runs per-use-case embedding queries, deduplicates by chunk ID (vector) or source_key (local), caps at 5 chunks, uses Promise.allSettled for resilience</done>
</task>

</tasks>

<verification>
- `node scripts/smoke.js` passes (existing 29 tests still green)
- `agents/solution.js` imports and exports unchanged
- Two new fixture files exist and are valid JSON
</verification>

<success_criteria>
- Per-use-case retrieval implemented per D-01/D-02/D-03
- 2 missing fixtures created for cybersecurity and full-stack scenarios
- Existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-solution-quality-rag-hardening/05-01-SUMMARY.md`
</output>
